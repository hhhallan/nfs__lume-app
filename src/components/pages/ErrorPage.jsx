import styled from "styled-components";

export default function ErrorPage() {
    return (
        <ErrorStyled>
            ERREUR
        </ErrorStyled>
    );
}

const ErrorStyled = styled.div`
  
`;